I1
M claims.lwc v=1.0.0
S ba=class=container bd=class=header be=class=stats-section bf=class=form-section bh=class=table-section bk=class=stat-card
F let recordId:never="";0 let accountId:never="";0 let claims:never=0;0 let isLoading:never=0;0 let headerEl:never=div(bd,h1("class=title","Claims Management"));0 let statTotal:never=div(bk,span("class=label","Total Claims"),span("class=value","0"));0 let statPending:never=div(bk,span("class=label","Pending"),span("class=value","0"));0 let statApproved:never=div(bk,span("class=label","Approved"),span("class=value","0"));0 let statsSection:never=div(be,statTotal,statPending,statApproved);0 let titleInput:never=input("type=text class=field");0 let submitBtn:never=button("type=submit onclick=handleSubmit class=btn-primary","Submit Claim");0 let claimForm:never=form(bf,h2("","New Claim"),titleInput,submitBtn);0 let tableHead:never=thead("",tr("",th("","ID"),th("","Title"),th("","Status"),th("","Amount")));0 let claimsTable:never=table("class=table",tableHead,tbody("class=body",""));0 let tableSection:never=section(bh,h2("","Claims List"),claimsTable);0 let claimsPage:never=div(ba,headerEl,statsSection,claimForm,tableSection);0 let hasClaims:never=claims;0 let getClaimCount:never=()->claims;0 let handleSubmit:never=()->claims;0 let handleRefresh:never=()->claims;0
