pub fn sumEvenSquares(n) =
  range(1, n + 1) |> filter(x -> x % 2 == 0) |> map(x -> x * x) |> fold(0, (+))

pub fn topN(ns, n) =
  ns |> sort |> reverse |> slice(0, n)

pub fn frequencies(tags) =
  tags |> map(t -> concat(t, ": "))
