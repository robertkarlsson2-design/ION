id: return-stmt
language: python
# Matches: return expr
# Emits:   the expression itself (return is implicit in Ion function bodies)
match:
  nodeType: return_statement
  children:
    - pattern:
        capture: $EXPR
emit:
  recurse: $EXPR
