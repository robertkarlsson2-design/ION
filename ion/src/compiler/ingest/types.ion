I1
M ingest.types v=0.0.0 d=core
S a=default b={ IonIRNode, IonIRModule } c=interface ConstructTrace { readonly layer: LayerName; readonly cstNode: CSTNode; readonly ionNode: IonIRNode; } d=interface CSTNode { readonly type: string; readonly text: string; readonly isNamed: boolean; readonly startPosition: { readonly row: number; readonly column: number }; readonly endPosition: { readonly row: number; readonly column: number }; readonly children: readonly CSTNode[]; } e=interface IngestError { readonly message: string; readonly cstNode: CSTNode; } f=interface IngestPlugin { readonly language: string; parse(source: string): CSTNode; } g=interface IngestResult { readonly module: IonIRModule; readonly traces: readonly ConstructTrace[]; readonly stats: LayerStats; readonly errors: readonly IngestError[]; } h=interface LayerStats { readonly pattern: number; readonly llm: number; readonly unhandled: number; } i=interface LLMFallbackHandler { translate(node: CSTNode, context: string): Promise<IonIRNode | null>; } j=interface PatternMatcher { readonly ruleId: string; match(node: CSTNode): IonIRNode | null; } k=interface PipelineConfig { readonly plugin: IngestPlugin; readonly patterns: readonly PatternMatcher[]; readonly llmFallback?: LLMFallbackHandler; /** Fully-qualified module name, e.g. 'org.acme.users'. */ readonly moduleName: string; /** Semver version string; defaults to '0.0.0'. */ readonly version?: string; } l=type LayerName = 'pattern' | 'llm';
F let a:never=b;0 let a:never=d;0 let a:never=l;0 let a:never=c;0 let a:never=h;0 let a:never=e;0 let a:never=g;0 let a:never=j;0 let a:never=i;0 let a:never=f;0 let a:never=k;0
