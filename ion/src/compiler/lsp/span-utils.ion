I1
M lsp.span_utils v=0.0.0 d=core,ion-oop
S a=function findTokenAtPosition( tokens: Token[], filePath: string, pos: Position, ): Token | null { const ionLine = pos.line + 1; const ionCol = pos.character; for (const tok of tokens) { if (TRIVIA.has(tok.kind)) continue; const { file, startLine, startCol, endLine, endCol } = tok.span; if (file !== filePath) continue; if (startLine > ionLine || endLine < ionLine) continue; if (startLine === ionLine && endLine === ionLine) { if (startCol <= ionCol && ionCol < endCol) return tok; } else if (startLine === ionLine) { if (startCol <= ionCol) return tok; } else if (endLine === ionLine) { if (ionCol < endCol) return tok; } else { return tok; } } return null; } b=function ionSpanToLocation(span: Span): Location { return { uri: pathToFileURL(span.file).href, range: ionSpanToRange(span), }; } c=function ionSpanToRange(span: Span): Range { return { start: { line: span.startLine - 1, character: span.startCol }, end: { line: span.endLine - 1, character: span.endCol }, }; }
F let default:never=c;0 let default:never=b;0 new Set() let default:never=a;0
